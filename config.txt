import os

class Config:
    # Render veya sunucu üzerindeki Environment değişkenini önceliklendir
    # Eğer bulunamazsa localhost için varsayılanı kullan
    SQLALCHEMY_DATABASE_URI = os.environ.get('DATABASE_URL') or \
        'mysql+pymysql://kullanici_adiniz:sifreniz@localhost/halic_antik_db'
    
    SQLALCHEMY_TRACK_MODIFICATIONS = False
    SECRET_KEY = os.environ.get('SECRET_KEY') or 'cok-gizli-bir-anahtar-kelime'